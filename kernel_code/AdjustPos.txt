typedef struct
{
  float3 pos;
  float4 orientation;
  float mass;
} Body;

__kernel void Adjust(__global Body *input)
{
  int gid = get_global_id(0);
  
  input[gid].pos = (float3)(gid);
  input[gid].orientation = (float4)(gid);
  input[gid].mass = gid;
}