typedef struct
{
  float3 pos;
  float4 orientation;
  float mass;
} Body;

__kernel void Adjust(__global Body *input)
{
  int gid = get_global_id(0);
  input[gid].pos += (float3)(1.0f, 1.0f, 1.0f);
  input[gid].orientation += (float4)(0.25f, 0.25f, 0.25f, 0.25f);
  input[gid].mass += 3.3f;
}