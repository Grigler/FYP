typedef struct
{
  float3 pos;
  float4 orientation;
  float mass;
} Body;

__kernel void Adjust(__global Body *input)
{
  int gid = get_global_id(0);
  
  input[gid].pos = (float3)(gid)*0.25f/0.165f;
  input[gid].orientation = (float4)(gid);
  input[gid].mass = gid*0.1654f/(gid+4365.0f);
}